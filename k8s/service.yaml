apiVersion: v1
kind: Service
metadata:
  name: devsecops-java-app-service
spec:
  selector:
    app: devsecops-java-app
  type: LoadBalancer   # Use NodePort for local clusters (minikube, kind)
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
